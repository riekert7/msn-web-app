services:
  web:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/app        # live code reload via --reload in command below
    command: gunicorn -b 0.0.0.0:8080 --timeout 120 --reload main:app
