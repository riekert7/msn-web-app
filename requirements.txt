Flask==3.0.2
gunicorn==21.2.0
google-cloud-storage==2.*
google-auth==2.*
google-api-python-client==2.*
requests==2.*
Werkzeug==3.*
python-dotenv==1.*

# Dev / test dependencies (not needed in production Docker image)
# pip install pytest pytest-flask

